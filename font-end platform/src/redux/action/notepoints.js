// 记录用户选择的id
export const notePoints=data=>({
    type:'notePoints',data
})

export const isSameF=data=>({   
    type:'sameFamily',data
})

export const addId=data=>({   
    type:'addId',data
})

export const clearMd5Id=data=>({   
    type:'clearMd5Id',data
})

// 选中的节点
export const addNodes=data=>({
    type:'addNodes',data
})

export const removeNodes=data=>({
    type:'removeNodes',data
})

export const clearNodes=data=>({
    type:'clearNodes',data
})
